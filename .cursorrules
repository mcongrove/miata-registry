For every TSX file, include the AGPL-3.0 license at the top of the file. Use TailwindCSS classes when styling. Use react-router-dom for routing. Don't include code comments to declare blocks of code.When referring to the cars in the registry, use the term "limited edition" instead of "special edition"; instead of "model", use "edition". Use the "brg" colors, never use "gray".